{
  "id": "personal-data-hub",
  "name": "Personal Data Hub",
  "description": "Unified interface to personal data through Peekaboo privacy gateway",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "hubUrl": {
        "type": "string",
        "description": "Base URL of the Peekaboo hub (e.g., http://localhost:7007)"
      },
      "apiKey": {
        "type": "string",
        "description": "API key for authenticating with the Peekaboo hub"
      }
    },
    "required": ["hubUrl", "apiKey"]
  },
  "uiHints": {
    "hubUrl": {
      "label": "Peekaboo Hub URL",
      "help": "The base URL where Peekaboo is running"
    },
    "apiKey": {
      "label": "API Key",
      "help": "API key generated from Peekaboo GUI settings",
      "sensitive": true
    }
  }
}
