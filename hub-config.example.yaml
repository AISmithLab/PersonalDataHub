# PersonalDataHub configuration
# OAuth credentials are fetched automatically during 'npx pdh init'.
# To use your own credentials instead, see systemdesigns/OAUTH-SETUP.md.
sources:
  gmail:
    enabled: true
    owner_auth:
      type: oauth2
      # Uncomment to use your own OAuth app:
      # clientId: "your-client-id.apps.googleusercontent.com"
      # clientSecret: "your-client-secret"

  google_calendar:
    enabled: true
    owner_auth:
      type: oauth2
      # Uncomment to use your own OAuth app:
      # clientId: "your-client-id.apps.googleusercontent.com"
      # clientSecret: "your-client-secret"

  github:
    enabled: true
    owner_auth:
      type: github_app
      # Uncomment to use your own OAuth app:
      # clientId: "your-github-client-id"
      # clientSecret: "your-github-client-secret"

# AI provider for policy translation (future feature).
# Uncomment one provider and add your API key.
# ai:
#   provider: anthropic
#   api_key: "your-anthropic-api-key"
#   model: "claude-sonnet-4-20250514"

# ai:
#   provider: openai
#   api_key: "your-openai-api-key"
#   model: "gpt-4o"

# ai:
#   provider: google
#   api_key: "your-google-ai-api-key"
#   model: "gemini-3-pro-preview"

port: 3000
